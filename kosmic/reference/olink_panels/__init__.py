"""Bundled Olink proteomics panel definitions.

JSON / XLSX files only. The Python that consumes them lives in
`kosmic.meta_analysis.olink_panels`. This module is a Python package
solely so `importlib.resources.files("kosmic.reference.olink_panels")`
can locate the data under pip-install / PyInstaller bundles.
"""
